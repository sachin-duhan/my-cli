// integerate it with BOXERS for cool msg!

- sudo apt install boxes

->  add git work
->  write code for shutdown and restart!
->  write code for study mode and define contraints!!


-> automatic build, run, commit of the last updated file in C++ competitive programming!

algorithm to follow!
Commit and stage the chages!
Use 
$ git diff-tree --no-commit-id --name-only -r {{commit_id}}

this will get the list of changed files after last commit!
if multiple files updated! 
	1. check for type specified!
	2. if multiple files of specified type are updated - then ask use for which one to build and check for output!

